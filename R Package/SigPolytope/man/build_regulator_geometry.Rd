% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GeometrySignatures_ERS.R
\name{build_regulator_geometry}
\alias{build_regulator_geometry}
\title{Build geometric latent representation for the regulatory signature}
\usage{
build_regulator_geometry(
  data,
  cols,
  id_col = "Circuitries_id",
  meta_cols = NULL
)
}
\arguments{
\item{data}{Tibble/data.frame com todas as colunas disponíveis.}

\item{cols}{Vetor de nomes de colunas de \code{data} que serão usadas
como coordenadas da geometria da assinatura (todas devem ser numéricas
ou coercíveis para numeric).}

\item{id_col}{Nome da coluna de ID dos circuitries (ex: "Circuitries_id").
Essa coluna NÃO entra na geometria; ela só identifica as linhas.}

\item{meta_cols}{Vetor de nomes de colunas de metadados a carregar junto
(ex: c("Pathways","Metabolism")). Se NULL, não traz metadados extras
além de \code{id_col}, se ele existir.}
}
\value{
Lista análoga à de build_signature_geometry(), mas para o regulador.
}
\description{
Idêntico a build_signature_geometry(), mas semanticamente voltado
ao lado regulador (o usuário escolhe outras colunas em \code{cols}).
}
